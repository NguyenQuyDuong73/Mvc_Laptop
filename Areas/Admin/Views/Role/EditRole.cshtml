@* @model EditRoleViewModel

@{
    ViewData["Title"] = "Sửa vai trò";
}

<h1>@ViewData["Title"]</h1>

<form asp-action="EditRole" method="post">
    <input type="hidden" asp-for="RoleId" />
    <div class="form-group">
        <label for="RoleName">Tên vai trò</label>
        <input type="text" class="form-control" asp-for="RoleName" />
    </div>

    <h3>Người dùng trong vai trò</h3>
    <table class="table">
        <thead>
            <tr>
                <th>Tên người dùng</th>
                <th class="text-center">Chọn</th>
            </tr>
        </thead>
        <tbody>
            @for (int i = 0; i < Model.Users.Count; i++)
            {
                <tr>
                    <td>@Model.Users[i].UserName</td>
                    <td class="text-center">
                        <input type="checkbox" asp-for="Users[i].IsSelected" />
                        <input type="hidden" asp-for="Users[i].UserId" />
                    </td>
                </tr>
            }
        </tbody>
    </table>

    <button type="submit" class="btn btn-primary">Lưu</button>
    <a asp-action="Index" class="btn btn-secondary">Hủy</a>
</form> *@
@model EditRoleViewModel

<h1>Sửa vai trò</h1>

<form asp-action="EditRole" method="post">
    <input type="hidden" asp-for="RoleId" />
    <div class="form-group">
        <label for="RoleName">Tên vai trò</label>
        <input type="text" class="form-control" asp-for="RoleName" />
        <span asp-validation-for="RoleName" class="text-danger"></span>
    </div>
    <button type="submit" class="btn btn-success">Lưu thay đổi</button>
</form>
@* <a asp-action="Index" class="btn-back mt-3"><i class="fas fa-arrow-left"></i>Trở lại</a> *@